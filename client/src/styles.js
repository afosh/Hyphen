import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  root: {
    background: "red",
    flexGrow: 1,
  },

  appBar: {
    display: "flex",
    // flexDirection: "column",
    color: "#000",
  },
}));
